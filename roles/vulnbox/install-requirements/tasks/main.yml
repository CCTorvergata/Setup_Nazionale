---
- name: Install requirements
  ansible.builtin.apt:
    name:
      - rsync
      - tshark
      - tmux
      - git
      - sudo
      - vim
      - nano
      - htop
    state: present
    update_cache: true
